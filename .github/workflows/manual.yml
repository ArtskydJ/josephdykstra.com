# generated by chatGPT from my .travis.yml file

name: Build and Deploy

on:
  push:
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up Node.js
        uses: actions/setup-node@v2
        with:
          node-version: 'node'

      - name: Install dependencies and run script
        run: |
          cd generator
          npm install
          node index

      - name: Commit and push changes
        run: |
          git config --global user.name "ArtskydJ"
          git config --global user.email "josephdykstra@gmail.com"
          git add .
          git commit -m "build [skip ci]"
          git push --quiet https://${{ secrets.GITHUB_TOKEN }}@github.com/ArtskydJ/josephdykstra.com master:master
